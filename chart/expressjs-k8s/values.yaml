# Default values for expressjs-k8s.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: acmpoc.azurecr.io/expressjs-k8s-service:latest
  pullPolicy: Always

service:
  port: 8080

